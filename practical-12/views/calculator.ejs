<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Kids Calculator 🧮</title>
    <style>
        body {
            font-family: 'Comic Sans MS', cursive, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            margin: 0;
            padding: 20px;
            min-height: 100vh;
        }
        
        .container {
            max-width: 500px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            padding: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }
        
        h1 {
            color: #5a67d8;
            text-align: center;
            font-size: 2.5em;
            margin-bottom: 30px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.1);
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        label {
            display: block;
            font-size: 1.2em;
            color: #4a5568;
            margin-bottom: 8px;
            font-weight: bold;
        }
        
        input[type="number"] {
            width: 100%;
            padding: 15px;
            font-size: 1.1em;
            border: 3px solid #e2e8f0;
            border-radius: 10px;
            box-sizing: border-box;
            transition: border-color 0.3s;
        }
        
        input[type="number"]:focus {
            outline: none;
            border-color: #5a67d8;
            box-shadow: 0 0 0 3px rgba(90, 103, 216, 0.1);
        }
        
        .operation-buttons {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin: 25px 0;
        }
        
        .operation-btn {
            background: none;
            border: 3px solid #e2e8f0;
            border-radius: 10px;
            padding: 15px;
            font-size: 1.1em;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s;
            font-family: inherit;
        }
        
        .operation-btn:hover {
            background: #f7fafc;
            transform: translateY(-2px);
        }
        
        .operation-btn.selected {
            background: #5a67d8;
            color: white;
            border-color: #5a67d8;
        }
        
        .calculate-btn {
            width: 100%;
            background: linear-gradient(135deg, #48bb78, #38a169);
            color: white;
            border: none;
            padding: 18px;
            font-size: 1.3em;
            font-weight: bold;
            border-radius: 12px;
            cursor: pointer;
            transition: transform 0.2s;
            font-family: inherit;
        }
        
        .calculate-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(72, 187, 120, 0.4);
        }
        
        .result {
            margin-top: 25px;
            padding: 20px;
            border-radius: 12px;
            text-align: center;
            font-size: 1.3em;
            font-weight: bold;
        }
        
        .result.success {
            background: #c6f6d5;
            color: #22543d;
            border: 2px solid #48bb78;
        }
        
        .result.error {
            background: #fed7d7;
            color: #742a2a;
            border: 2px solid #e53e3e;
        }
        
        .emoji {
            font-size: 1.5em;
            margin: 0 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🧮 Kids Calculator 🧮</h1>
        
        <form method="POST" action="/calculate">
            <div class="form-group">
                <label for="num1">First Number:</label>
                <input type="number" 
                       id="num1" 
                       name="num1" 
                       value="<%= locals.num1 || '' %>" 
                       placeholder="Enter your first number"
                       step="any"
                       required>
            </div>
            
            <div class="form-group">
                <label for="num2">Second Number:</label>
                <input type="number" 
                       id="num2" 
                       name="num2" 
                       value="<%= locals.num2 || '' %>" 
                       placeholder="Enter your second number"
                       step="any"
                       required>
            </div>
            
            <div class="form-group">
                <label>Choose Operation:</label>
                <div class="operation-buttons">
                    <button type="button" 
                            class="operation-btn <%= locals.operation === 'add' ? 'selected' : '' %>" 
                            onclick="selectOperation('add')">
                        ➕ Add
                    </button>
                    <button type="button" 
                            class="operation-btn <%= locals.operation === 'subtract' ? 'selected' : '' %>" 
                            onclick="selectOperation('subtract')">
                        ➖ Subtract
                    </button>
                    <button type="button" 
                            class="operation-btn <%= locals.operation === 'multiply' ? 'selected' : '' %>" 
                            onclick="selectOperation('multiply')">
                        ✖️ Multiply
                    </button>
                    <button type="button" 
                            class="operation-btn <%= locals.operation === 'divide' ? 'selected' : '' %>" 
                            onclick="selectOperation('divide')">
                        ➗ Divide
                    </button>
                </div>
                <input type="hidden" id="operation" name="operation" value="<%= locals.operation || '' %>">
            </div>
            
            <button type="submit" class="calculate-btn">
                🚀 Calculate!
            </button>
        </form>
        
        <% if (result !== null) { %>
            <div class="result success">
                <span class="emoji">🎉</span>
                The answer is: <strong><%= result %></strong>
                <span class="emoji">🎉</span>
            </div>
        <% } %>
        
        <% if (error) { %>
            <div class="result error">
                <span class="emoji">😅</span>
                <%= error %>
                <span class="emoji">😅</span>
            </div>
        <% } %>
    </div>
    
    <script>
        function selectOperation(op) {
            document.querySelectorAll('.operation-btn').forEach(btn => {
                btn.classList.remove('selected');
            });
            
            event.target.classList.add('selected');
            
            document.getElementById('operation').value = op;
        }
        
        document.querySelector('form').addEventListener('submit', function(e) {
            const operation = document.getElementById('operation').value;
            if (!operation) {
                e.preventDefault();
                alert('Please select an operation! 😊');
            }
        });
    </script>
</body>
</html>
